SEO / indexing (2026-09-18 CT)
==============================
Branch: review/sdc-seo
Base: live main @ hub-chrome tip

Static: unique title + meta description + OG/Twitter + canonical
  https://01ls1z28-coder.github.io/sound-distance-calculator/
robots.txt Allow + sitemap.xml; titlebar-label → semantic <h1> (margin:0).
Disclaimers / OSHA / sources / non-affiliation unchanged. No JS/data/deploy.

VERIFY
  1. View-source: title/description/canonical/OG/Twitter present; one H1
  2. /robots.txt + /sitemap.xml resolve after Pages publish
  3. Layout chrome unchanged; no chart math change

Intentional skips: deploy; tracking; fleet/data retune.

Sound Distance Calculator — verification notes
==============================================

FAMILY BRASS — SDC brass chrome (2026-09-11 CT)
-----------------------------------------------
CSS/theme only. Ports VelocityBench Phase 13/14 `--brass*` tokens so SDC
titlebar/panels/inputs read as the same instrument-cluster family as VB + hub.

Applied (exact VB token names):
  --brass, --brass-strong, --brass-inset, --brass-text,
  --brass-face, --brass-shadow  (+ existing --carbon-fine / --panel for face stack)

Chrome surfaces:
  - .titlebar: brass face + bottom bezel + shadow
  - .titlebar-mark: brass-text ring (VB logo-mark language)
  - .titlebar-sub / credit label: brass-text accents
  - .panel: brass face/border/shadow, top highlight (::before),
    Phase 14 corner ticks (::after)
  - Inputs/selects: brass bezel + inset; brass focus ring
  - Specs / ranked list / panel captions / distance control / chart-notes /
    table header: brass borders + label accents
  - Jorge Guerra credit <em> stays lime (high-contrast identity)
  - Distance slider value/thumb keep cyan/lime contrast

Intentional skips:
  - SPL table band colors (red/yellow/green/gray) — functional readability
  - Chart.js canvas styling / OSHA line math — not chrome
  - Product copy, marks, credits, OSHA/disclaimer, sources unchanged
  - No HTML hooks required; no analytics; no JS/data changes

Verify:
  1. Open index.html locally
  2. Side-by-side with VB tool cards / hub — champagne bezel language matches
  3. Corner ticks light on panels; selects readable; lime credit name intact
  4. Mobile: stacked panels, no horizontal overflow from ticks

Hub chrome 2026-09-18 CT: css/vb-chrome.css + nav on index (Hub=velocitybench.com, peers absolute).
